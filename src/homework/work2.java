package homework;

public class work2 {
    public static void main(String[] args) {
// Assign boolean values according to statements

// Java is a programming language
        boolean isJava =true; // Since above statement is correct value assigned to true
// you should do same  for every variable down below and please do not print anything anytime

        // There are 8 primitive data types.
        boolean is8primitive =true;

// Char is a primitive data type.
        boolean isCharPrimitive =true;


// In java, to be able to define texts we use double quotes.
        boolean isDoubleQuoteText =true;

// When we define double variable we must use d letter at the end of number.
        boolean ISdNecessary = false;

// When we assign float variable with decimal value we must put f at the end.
        boolean isFneeded =true;


// String is a primitive data type.
        boolean isStringPrimitive = true;

// We can start naming with numbers
        boolean doesNameStartWithNumber =false;


// When I am creating a double value I can use _(underscore) between the digits
        boolean isUnderscore =false;

// I can assign boolean value with "true" like boolean bl = "true"
        boolean isAssign =true;

// In java I can run my code without using main method
        boolean isWithoutMain = true;

// When we declaring long variable and assigning with the value which is not bigger than int we must put l at
// the end of the number.
        boolean isLneeded = false;

        System.out.println(is8primitive);
        System.out.println(isCharPrimitive);
        System.out.println(isDoubleQuoteText);
        System.out.println(ISdNecessary);
        System.out.println(isFneeded);
        System.out.println(isStringPrimitive);
        System.out.println(doesNameStartWithNumber);
        System.out.println(isUnderscore);
        System.out.println(isAssign);
        System.out.println(isWithoutMain);
        System.out.println(isLneeded);

    }
}

